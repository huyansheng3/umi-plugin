import Vue from 'vue';
import umiVue from '@ddot/umi-vue';

const runtimeVuex = window.g_plugins.mergeConfig('vuex');

let app = umiVue({
  ...(runtimeVuex.config || {}),
});

window.g_app = app;

{{{RegisterPlugins}}}
{{{RegisterModels}}}

